"""
api/auth.py
-----------
Auth stub — no API key required for this deployment.
The verify_api_key dependency is kept so route signatures don't change.
"""


def verify_api_key() -> None:
    return None
